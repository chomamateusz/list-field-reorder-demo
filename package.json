{
  "name": "list-field-reorder-demo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@bit/amazingdesign.uniforms.list-field-reorder": "0.0.1",
    "ajv": "^6.10.2",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.2.0",
    "uniforms": "^2.5.0",
    "uniforms-bridge-json-schema": "^2.5.0",
    "uniforms-material": "^2.5.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
